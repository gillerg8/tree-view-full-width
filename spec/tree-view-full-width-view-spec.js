'use babel';

import TreeViewFullWidthView from '../lib/tree-view-full-width-view';

describe('TreeViewFullWidthView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
